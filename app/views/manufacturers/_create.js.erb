$("ul.errors").html("");
<% if @manufacturer.errors.any? %>
  <% @manufacturer.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".manufacturers").html("<%= escape_javascript(render 'index') %>");
  $("#manufacturer-modal").modal("hide");
 
<% end %>