$("ul.errors").html("");
<% if @park.errors.any? %>
  <% @park.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".parks").html("<%= escape_javascript(render 'index') %>");
  $("#park-modal").modal("hide");
 
<% end %>