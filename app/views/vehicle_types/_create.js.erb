$("ul.errors").html("");
<% if @vehicle_type.errors.any? %>
  <% @vehicle_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".vehicle-types").html("<%= escape_javascript(render 'index') %>");
  $("#vehicle-type-modal").modal("hide");
 
<% end %>